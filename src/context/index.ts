export { useWorkspace, WorkspaceProvider } from "./workspace-context";
export type {
    FileEntry, WorkspaceContextValue, WorkspaceState
} from "./workspace-context";
export type { PinnedWorkspace } from "@/lib/settings";
